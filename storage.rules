rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // File uploads stay closed until an audited upload workflow is installed.
    match /{allPaths=**} { allow read, write: if false; }
  }
}
